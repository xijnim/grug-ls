counter_id: id = spawn_counter("shots_counter")

on_spawn() {
    set_gun_name("RPG-7")
    set_gun_sprite_path("rpg-7.png")
    set_gun_rounds_per_minute(120)
    set_gun_companion("vanilla:crate")

    set_gun_rounds_per_minute(3)

    shots: i32 = 0
    thing: string = "hi"
    file: resource = thing

    map_set_i32(counter_id, "shots", 0)
}

on_despawn() {
    despawn_entity(counter_id)
}

on_fire() {

}

helper_hi(name: string) string {
}
